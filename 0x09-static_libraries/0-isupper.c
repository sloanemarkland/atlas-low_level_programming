#include "main.h"
/**
* _isupper - printing alphabet uppercase
* @c: if upper, return 1 $mrklnd
* Return: Always 0
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
