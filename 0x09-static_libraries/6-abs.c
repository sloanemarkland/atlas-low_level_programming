#include "main.h"

/**
* _abs - absolute value of int
* @n: if pos num, print, else * -1 $mrklnd
* Return: Always 0
*/

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (n * -1);
	}
}
