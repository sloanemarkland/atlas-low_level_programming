#include "main.h"

/**
* main - oo na na, print my name; write func that prints its own name
* @argc: num of command-line args passed to program
* @argv: array of pointers to command-line args
* $mrklnd
* Return: 0
*/
int main(int argc, char *argv[])
{
	if (argc)
	{}
		printf("%s\n", argv[0]);
		return (0);

}
