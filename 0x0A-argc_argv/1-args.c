#include "main.h"

/**
* main - print num of args passed into function
* @argc: num of command-line args passed to program
* @argv: array of pointers to command-line args
* $mrklnd
* Return: 0
*/

int main(int argc, char *argv[])
{
	if (argv)
	{}
		printf("%d\n", argc - 1);
		return (0);

}
